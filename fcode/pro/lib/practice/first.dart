import 'package:flutter/material.dart';

//just run the code
void main() {
  runApp(

      Center(
        child: Text(
          "First Page",
          textDirection: TextDirection.ltr,
        ),
      )
  );
}
